```cudaGraph_t``` 对象不是线程安全的。用户有责任确保多个线程不会同时访问同一个```cudaGraph_t```。

一个```cudaGraphExec_t```不能与自身并发运行。同一可执行图的先前启动后，```cudaGraphExec_t```的启动将按顺序进行。

图执行在流中进行，以便与其他异步工作进行排序。然而，该流仅用于排序；它不会限制图的内部并行性，也不会影响图节点的执行位置。
