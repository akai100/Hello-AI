
在现代计算机中，浮点数是按照IEEE 754标准来表示的。有关浮点运算和IEEE 754标准的更多详细信息，请参阅浮点运算。
尤其需要注意的是，浮点数的精度有限（单精度浮点数约为7位小数，双精度浮点数约为16位小数），而且浮点加法和乘法不满足结合律，因此运算顺序会影响结果。
正因为如此，对于数学上完全相同的浮点计算，PyTorch并不保证能产生完全相同的位级结果。
同样，在不同的PyTorch版本、各个提交版本或不同平台之间，也不能保证结果在位级上完全一致。
特别是，即使输入在位级上完全相同，并且在控制了随机性来源之后，CPU和GPU的计算结果也可能不同。
